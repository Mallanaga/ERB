<%= form_for(@company) do |f| %>
  <%= render 'companies/fields', f: f %>
  <br/><br/>
  <%= f.submit "Create Company", class: "btn btn-large btn-primary" %>
  <%= f.submit "Cancel", data: { confirm: "All changes will be lost.  Are you sure?" }, class: 'btn' %>
<% end %>