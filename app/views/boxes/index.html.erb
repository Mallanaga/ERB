<div class='headercontent'>
  <%= link_to "Export CSV", boxes_path(format: "csv") %> | 
  <%= form_tag import_boxes_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag "Import CSV" %>
  <% end %>

  <%= render @boxes %>
  <%= paginate @boxes %>
</div>